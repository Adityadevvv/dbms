SET SERVEROUTPUT ON;

DECLARE
    num NUMBER := &num;  -- take input from user
BEGIN
    IF MOD(num, 2) = 0 THEN
        DBMS_OUTPUT.PUT_LINE(num || ' is even.');
    ELSE
        DBMS_OUTPUT.PUT_LINE(num || ' is odd.');
    END IF;
END;
/
